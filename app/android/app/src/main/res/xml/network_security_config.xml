<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is off everywhere, with one exception.

  Android already defaults to this on modern target SDKs, but the default is a
  property of the SDK level rather than a decision, and this connection carries
  passwords, tokens and the record of what somebody reads. Stating it here
  means it survives a target-SDK change.

  The exception is the host addresses that mean "the machine this emulator is
  running on". They are how a developer reaches `dotnet run`, and they are
  unreachable from a real device, so the exception cannot widen in the field.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <!-- The Android emulator's alias for the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Genymotion's. -->
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
